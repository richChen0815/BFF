关于用户绑定前台设计改动说明：
原有设计：
    逻辑：根据接口返回用户是否绑定过状态，前台根据状态值控制跳转。
    范围：C端和Pc端所有的学习模块都需要检测用户是否绑定。
    binding状态缓存时机：前端项目初始化中调用用户是否绑定接口来缓存接口返回状态。
    中间态修改触发事件：用户重新打开页面或者刷新页面触发逻辑。
    修改缓存状时机：1.项目初始化缓存状态
                 2. 绑定手机号后修改缓存状态
    缓存作用：减少的接口重复调用，避免网络带宽以及io性能消耗。

调整一：希望正在学习的用户不受影响
    调整逻辑：增加开发逻辑，先判断用户是否需不需要开启绑定功能，在具体的某个时间段开启，减少对用户的影响，跳转逻辑不变。

调整二：分阶段控制不同的课程是否需要绑定
    调整逻辑一：原来的根据”开关接口“和“用户是否绑定接口”判断是否跳转 改为 根据"判断当前场景下是否需要绑定手机号"接口 拿到用户是否需要跳转。
     
    缓存逻辑改动
        说明：用户是否需要跳转，是异步计算过程，前端项目初始化拿到的是否跳转状态不能立马缓存，是否缓存的逻辑如下：
        1. 项目初始化时，如果用户已经绑定状态 ，前端缓存 状态值，不跳转绑定页面(取消路由跳转监控)。
        2. 如果用户为未绑定状态，当路由发生变化过程中，需要再次通过接口查询判断用户是否需要跳转。
        如果接口返回不需要跳转标志，前端缓存状态值。否则不缓存，路由变化时再次查询跳转状态。

        前端“是否跳转”接口查询次数：
            1.如果已绑定 不需要再次查跳转接口   0次
            2.如果未绑定 查询接口次数根据 “用户是否需要跳转”的计算结果  以及 页面路由是否发生变化决定 。

        

            
